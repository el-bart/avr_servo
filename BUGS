* sending "os00?\n" kills the software - it stops responsing, though watdog
  does not react as well. sending commands to last 6 servos does the same.
* when running PanTiltApp demo program system randomly crashes with assertion
  from USART's interrupt on data arrival, that input queue pointer is NULL.
