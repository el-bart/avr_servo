* driver VERY often returns timeouts on responses - this can be an issue with an AVR or driver itself. investigate this...
* when moving one servo, others do move as well (they shake a little) - it must be fixed!
